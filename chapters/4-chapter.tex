\chapter{Solução Proposta e Metodologia Utilizada}

Neste capítulo será discutida a solução que foi avaliada como a mais apropriada para resolução do problema do projeto. 

O primeiro ponto a ser considerado é a metodologia de desenvolvimento da solução como um todo, vista como um problema de mineração de dados. Além disso, é feita desde uma análise detalhada dos dados utilizados e do objeto de interesse (placas de veículos), ao detalhamento do algoritmo de leitura de placas de carro que foi desenvolvido.

Também são discutidas as soluções utilizadas para adaptação do algoritmo visando a sua execução na placa Rock 3A, incluindo as tecnologias e processos necessários.

Por fim, é considerada a metodologia de teste da solução, os passos necessários para evitar viés nos resultados e garantir relevância estatística para os números obtidos.

\section{Metodologia de desenvolvimento}

A metodologia de desenvolvimento da solução como um todo segue o padrão CRISP-DM, discutido no capítulo 2, que trata o projeto como um problema de mineração de dados. Nesse padrão, o primeiro passo para o desenvolvimento do projeto é o entendimento do problema de negócio, que já foi discutido e bem definido no capítulo 3.

As etapas do projeto definidas a permitir do padrão CRISP-DM são portanto:

\begin{enumerate}
    \item \textbf{Entendimento do problema} \\
    O problema de negócio foi bem definido e explorado no capítulo 3, tendo como objetivo geral \textit{"Desenvolver um software de leitura automática de placas de veículos e validar a viabilidade funcional de uma solução de baixo custo que execute esse software em uma placa Rock 3A."}
    \item \textbf{Entendimento dos dados} \\
    Para obter um bom entendimento dos dados é necessário explorar as características tanto do \textit{dataset} utilizado quanto do objeto de interesse a ser analisado, as placas de veículos. Esse estudo será feito na próxima seção deste capítulo.
    \item \textbf{Preparação dos dados} \\
    A preparação dos dados envolve duas etapas distintas, pois esse projeto lida com dois conjuntos de dados, sendo o segundo derivado do primeiro. O primeiro conjunto são as imagens dos carros e anotações das placas, e a preparação desses dados envolve separar o \textit{dataset} em conjuntos de treino, validação e teste, além de organizá-los e transformá-los para que fiquem no padrão COCO \cite{coco}. Essa organização e transformação é necessária para que o modelo de detecção de placas seja treinado. \\ 
    A segunda etapa da preparação dos dados ocorre posteriormente e envolve a criação de um segundo conjunto de dados gerado a partir do primeiro. Esse segundo conjunto de dados deve ser um \textit{dataset} de imagens de caracteres de placas de carro, voltado para o treinamento de um modelo de classificação de caracteres. Para realização dessa tarefa, porém, é necessário que já tenha havido um avanço na modelagem, de forma que a etapa de preparação dos dados e de modelagem possuem alternância.
    \item \textbf{Modelagem} \\
    A modelagem, por sua vez, pode ser dividida em três etapas principais. A primeira é a seleção e treinamento de um modelo de detecção de objetos para a detecção das placas, etapa essa que depende da preparação do primeiro conjunto de dados, contendo as imagens dos carros e anotações das placas. \\
    A segunda etapa, que depende da primeira, é o desenvolvimento de um algoritmo para extração dos caracteres das placas identificadas pelo modelo de detecção de objetos, utilizando técnicas clássicas de visão computacional. Após essa etapa é possível (e necessária) a realização da segunda parte da preparação dos dados, que gera o \textit{dataset} de caracteres extraídos. \\
    Por fim, a última etapa de modelagem é a criação e treinamentos de modelos de redes neurais de classificação de caracteres, que devem ser capazes de identificar caracteres a partir das imagens extraídas pelo algoritmo de extração de caracteres desenvolvido.
    \item \textbf{Avaliação} \\
    A avaliação envolve realizar testes com o algoritmo completo e extrair métricas de desempenho. Esses testes devem ser realizados tanto na CPU e GPU do PC quanto na CPU e NPU da placa Rock 3A, e avaliar tanto a confiabilidade do modelo em inferir corretamente as placas dos veículos quanto a velocidade de inferência.
    \item \textbf{\textit{Deployment}} \\
    No caso deste projeto o \textit{deployment} nada mais é do que realizar um relatório final dos resultados para validar o modelo e o \textit{hardware} como viáveis ou não para o desenvolvimento de um produto de leitura automática de placas veiculares. Esse relatório estará contido no capítulo 5.
\end{enumerate}

\section{Análise dos dados}

Antes que seja possível desenvolver qualquer modelo de leitura de placas veiculares é necessário entender as características das placas a serem analisadas, além das características do \textit{dataset} específico a ser utilizado no desenvolvimento.

Nesta seção serão exploradas as diferentes variações de placas de carro existentes no Brasil que serão encontradas na nossa análise, e também os atributo dos dados a serem utilizados, como o formato das anotações e descrição da metodologia de coleta.

\subsection{Características do objeto}

O sistema de placas de carros do Brasil já passou por diversas alterações ao longo dos anos, sendo que a última mais significativa foi a adoção do "padrão Mercosul", em 2018. Desde então, dois padrões de placas veiculares seguem coexistindo, o padrão Mercosul e o padrão anterior, de 1990, que segue em circulação embora não sejam mais emitidas novas placas nesse formato.

O padrão antigo, iniciado em 1990, é um padrão alfanumérico de 7 caracteres, que seguem o formato ABC·1234. Três letras e um número de 4 dígitos, com um ponto ou hífen separando os dois blocos. 

As placas dessa modalidade costumam ser da cor cinza metálico com caracteres pretos (figura \ref{fig:placabr}), mas existem exceções importantes. Nesse padrão, algumas categorias de veículos utilizam placas de fundo colorido e caracteres brancos. A mais comum dessas categorias é a placa utilizada em veículos que realizam transporte pago, cujo fundo é vermelho (figura \ref{fig:placabrvermelha}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/placa-br.jpg}
    \caption{Modelo de placa veicular do Brasil de 1990}
    \label{fig:placabr}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/placavermelha-br.png}
    \caption{Modelo de placa veicular do Brasil de 1990 para veículos que realizam transporte pago}
    \label{fig:placabrvermelha}
\end{figure}

As placas novas, do padrão Mercosul, utilizam uma sequência alfanumérica diferente. O formato é ABC1D23, que contém 4 letras, um número de 1 dígito e outro número de 2 dígitos, sendo que a quarta letra fica entre os dois números. Esse formato permite mais combinações diferentes que o anterior.

As placas do padrão Mercosul possuem sempre fundo branco, enquanto as letras podem variar em cor a depender da modalidade de veículo, sendo preto a mais comum (figura \ref{fig:placame}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/placa-me.jpg}
    \caption{Modelo de placa veicular do padrão Mercosul}
    \label{fig:placame}
\end{figure}

Ambos modelos de placas possuem informações adicionais na parte de cima, notadamente o padrão de 1990 inclui a cidade e UF do veículo e o padrão Mercosul inclui o país. Além disso, as placas do padrão Mercosul incluem símbolos e selos adicionais que ajudam na verificação de autenticidade. Os dois modelos possuem margens ao redor do texto principal.

\subsection{Características do \textit{dataset} utilizado}

O conjunto de dados utilizado foi RodoSol-ALPR, criado no trabalho \cite{dataset}. Esse \textit{dataset} é composto por 10.000 imagens de carros e 10.000 imagens de motocicletas com dimensão de 1.280 x 720 px. As imagens, além de serem classificadas como "carro" ou "moto", também são classificadas entre veículos com placa no padrão antigo (BR) e com placa no padrão Mercosul (ME), sendo que metade das imagens tanto de carros quanto de motos pertencem a cada padrão de placa.

As imagens desse \textit{dataset} foram capturadas por câmeras estáticas em um pedágio na Rodovia do Sol, no Espírito Santo. Inclui imagens com iluminação boa e ruim, com chuva e sem, além de carros em diferentes distancias da câmera.

As anotações das imagens são no seguinte formato:

\begin{verbatim}
type: car
plate: ODE2510
layout: Brazilian
corners: 558,438 687,439 687,482 558,481
\end{verbatim}

A primeira linha especifica o tipo de veículo (\textit{car/motorcycle}), a segunda, os caracteres da placa, a terceira o layout da placa (\textit{Brazilian/Mercosur}) e a quarta as coordenadas dos 4 cantos da placa em pixels.

A figura \ref{fig:inputexemplo} é um exemplo de imagem do \textit{dataset} com as marcações dos 4 cantos da placa obtidas do arquivo de anotação.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/exemplo-input.png}
    \caption{Exemplo de imagem do \textit{dataset} com as marcações dos 4 cantos da placa}
    \label{fig:inputexemplo}
\end{figure}

\section{\textit{Pipeline} de leitura de placas}

O desenvolvimento do \textit{pipeline} de leitura de placas já faz parte da etapa de modelagem do método CRISP-DM. O objetivo do \textit{pipeline} é receber imagens de veículos e devolver uma sequência de caracteres correspondente à placa de identificação do veículo contido na imagem.

Essa tarefa não é um problema trivial de visão computacional, e dificilmente pode ser resolvida simplesmente alimentando as imagens a um modelo de redes neurais artificiais. A complexidade se dá principalmente pelos seguintes fatos:

\begin{itemize}
    \item A placa pode estar localizada em locais variados da imagem e ter diferentes tamanhos
    \item A quantidade de placas possíveis é aproximadamente $4.6 \times 10^8$ para placas do padrão Mercosul e $1.8 \times 10^8$ para placas no padrão antigo, sendo impossível treinar um modelo no qual cada placa seja tratada como uma classe distinta
    \item A maior parte do conteúdo de cada imagem é apenas ruído que não pode ser aproveitado para a leitura da placa
\end{itemize}

Por conta da complexidade, o problema requer uma solução refinada. Com base nisso, o \textit{pipeline} proposto para a leitura de placas pode ser dividido em três blocos sequenciais:

\begin{enumerate}
    \item \textbf{Detecção e classificação da placa} \\
    O bloco de detecção e classificação da placa deve extrair da imagem de entrada a localização exata da imagem, permitindo que todo o resto da imagem possa ser descartado ne etapa seguinte. Além disso, esse bloco deve classificar a placa entre os modelos Brasil e Mercosul e identificar as coordenadas cartesianas dos quatro canto da placa.
    \item \textbf{Processamento da placa} \\
    Este bloco deve realizar transformações na imagem utilizando as informações obtidas pelo modelo de detecção. Essas transformações envolvem cortar a imagem, para remover tudo o que não seja a placa, e também outras operações de processamento que facilitem a leitura dos caracteres no bloco seguinte.
    \item \textbf{Leitura da placa} \\
    Por fim, o último bloco do \textit{pipeline} recebe a imagem processada e aplica o algoritmo de leitura para efetivamente extrair os caracteres da placa.
\end{enumerate}

A seguir serão exploradas as soluções propostas para cada bloco.

\subsection{Modelo de detecção de objetos e pontos-chave}

Para a tarefa de detecção e classificação da placa, a solução proposta foi utilizar um modelo de detecção de objetos da família YOLO, mais especificamente o YOLO26-pose, desenvolvido pela Ultralytics.

Os modelos da família YOLO são notórios por serem precisos e rápidos na detecção de objetos. Nessa linha, os modelos YOLO26 foram desenvolvidos pensando especificamente em IA de borda, sendo portanto otimizados para entregar excelente performance com baixa latência e utilizando o mínimo de recursos.

Uma das variantes disponíveis do YOLO26 é o YOLO26-pose, que além de realizar detecção de objetos também realiza detecção de pontos-chave na imagem, sendo portanto ideal para o caso de uso desse projeto.

O YOLO26-pose ainda possui 5 tamanhos, apresentados na tabela \ref{tab:yolo26_table}.

\begin{table}[H]
    \centering
    \begin{tabular}{| m{3cm} | m{3cm} | m{3cm} | m{3cm} |}
        \hline
        Tamanho & Parâmetros (M) & Velocidade CPU (ms) & mAPpose 50-95(e2e) \\
        \hline \hline
        \textit{nano} & $2.9$ & $40.3$ & $57.2$ \\
        \hline
        \textit{small} & $10.4$ & $85.3$ & $63.0$ \\
        \hline
        \textit{medium} & $21.5$ & $218.0$ & $68.8$ \\
        \hline
        \textit{large} & $25.9$ & $275.4$ & $70.4$ \\
        \hline
        \textit{extra large} & $57.6$ & $565.4$ & $71.6$ \\
        \hline
    \end{tabular}
    \caption{Tamanhos do modelo de detecção de objetos YOLO26-pose}
    \label{tab:yolo26_table}
\end{table}

A métrica mAPpose 50-95(e2e) representa a precisão média para estimativa de pose (pontos chave). Nota-se que quanto maior o modelo mais alta é a precisão, porém mais alta também é a latência. Para este projeto, como estamos lidando com um objeto relativamente simples (placas de carro) e o \textit{hardware} é bastante limitado, foi escolhida a versão \textit{nano} do modelo, que prioriza velocidade de inferência e economia de recursos.

\subsection{Algoritmo de processamento da imagem}

Uma vez detectada a placa, incluindo as coordenadas dos seus 4 cantos na imagem, torna-se possível realizar diversas transformações na imagem para facilitar a tarefa de leitura dos caracteres da placa. Essas transformações devem ter como objetivo extrair o máximo de informação e remover o máximo de ruído possível, de forma a entregar para a etapa seguinte dados mais limpos, informativos e padronizados.

Com esse objetivo, a sequência proposta de transformações é a seguinte:

\begin{enumerate}
    \item Conversão da imagem para escala de cinza
    \item Extração da placa com base na \textit{bounding box}
    \item Transformação de perspectiva
    \item Remoção das bordas
    \item Binarização
    \item Separação dos caracteres
    \item Limpeza das imagens de caracteres
\end{enumerate}

A seguir é discutida cada uma dessas etapas.

\subsubsection{Conversão para escala de cinza}

Enquanto as cores poderiam ser úteis para detectar a placa na imagem original, elas já não têm muito propósito a partir desse ponto, pois as placas praticamente não têm cores, e quando têm elas não são relevantes. Por conta disso, as cores são informação desnecessária que só complicaria as etapas posteriores, de forma que é melhor eliminá-las.

Ao converter a imagem para escala de cinza (figura \ref{fig:inputgray}) ela passa a ser representada por uma matriz $h \times w$ (sendo $h$ a altura da imagem e $w$ a largura), ao invés de três matrizes, como era quanto tinha cores.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/exemplo-gray.png}
    \caption{Imagem em escala de cinza}
    \label{fig:inputgray}
\end{figure}

\subsubsection{Extração da placa com base na \textit{bounding box}}

A \textit{bounding box} de um objeto em uma imagem nada mais é do que os limites superior, inferior, direito e esquerdo do objeto. Com essas quatro informações é possível formar um retângulo que, idealmente, contém o objeto inteiro sem ser maior do que o necessário.

Uma das saídas do modelo de detecção de objetos é a \textit{bounding box} de cada objeto detectado na imagem. É importante notar que é possível que mais de um objeto seja detectado em uma dada imagem, caso no qual apenas a melhor detecção será utilizada, ou seja, a que tiver a confiança mais alta.

Com base na \textit{bounding box} obtida e na imagem em escala de cinza é gerada uma nova imagem, formada pelos pixels da imagem contidos dentro da \textit{bounding box}. Essa nova imagem tem, portanto, as mesmas dimensões da \textit{bounding box}.

A figura \ref{fig:extracaoplaca} exemplifica o processo de extração da placa com base na \textit{bounding box} inferida pelo modelo de detecção de objetos.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/extracao_placa.png}
    \caption{Extração da placa com base na \textit{bounding box}}
    \label{fig:extracaoplaca}
\end{figure}

\subsubsection{Transformação de perspectiva}

Após extrair a imagem da placa, o próximo passo é utilizar a transformação de perspectiva, de forma que esta fique sem rotação e sem perspectiva. Essa operação tem grande potencial de padronização das imagens de placa, anulando variações que possam ocorrer referentes à posição do carro.

Essa transformação é feita com base nas coordenadas dos quatro cantos da placa, que definem o plano inicial, assim como as coordenadas dos quatro cantos na imagem final.

Para realizar isso é necessário primeiro realizar uma transformação nas coordenadas dos cantos para que o ponto de referência deixe de ser a origem da imagem inicial (do carro inteiro) e passe a ser a origem da imagem da placa.

Para isso, basta aplicar as equações:

\begin{gather}
    x_2 = x_1 - x_{min} \\
    y_2 = y_1 - y_{min}
\end{gather}

Onde $(x_1,y_1)$ são as coordenadas de um ponto referente à imagem do carro e $(x_2,y_2)$ são as coordenadas referentes à imagem recortada da placa. $(x_{min},y_{min})$ são os limite inferior de $x$ e $y$ na \textit{bounding box}.

Por fim, basta definir as dimensões da imagem após a transformação. Essa definição é relativamente arbitrária, porém é ideal que a imagem final possua um tamanho grande o bastante para que não haja queda na resolução e que as proporções da largura e altura não sejam muito diferentes das proporções de uma placa de carro padrão, para que não ocorra muita distorção na imagem. Foi proposta então as dimensões de $500 \times 200$ px.

A figura \ref{fig:pespectransform} exemplifica o que acontece com a imagem da placa após a transformação. Além de a placa se ajustar muito melhor na imagem, suas dimensões ficam padronizadas em $500 \times 200$ px.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/trans-perspec.png}
    \caption{Transformação de perspectiva com base nos quatro cantos da placa}
    \label{fig:pespectransform}
\end{figure}

\subsubsection{Remoção das bordas}

Com as dimensões da imagem padronizadas e a perspectiva corrigida, é possível, com base em conhecimento empírico acerca das características dimensionais da placa, reduzir ainda mais a imagem ao remover bordas desnecessárias, mantendo, na medida do possível, apenas os caracteres.

Essa operação é diferente em placas do modelo Brasil e Mercosul, pois estes modelos são diferentes nas dimensões das bordas. Nas placas do modelo Brasil são removidos 40 pixels do topo e 10 pixels da parte de baixo, além de 10 pixels na esquerda e na direita. Já nas placas do modelo Mercosul são removidos 40 pixels do topo, da esquerda e da direita, além de 10 pixels da parte de baixo. As figuras \ref{fig:corte-br} e \ref{fig:corte-me} mostram as medidas removidas das placas.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{images/corte-placa-br.png}
    \caption{Medidas para remoção de bordas das placas do padrão antigo}
    \label{fig:corte-br}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{images/corte-placa-me.png}
    \caption{Medidas para remoção de bordas das placas do padrão Mercosul}
    \label{fig:corte-me}
\end{figure}

A figura \ref{fig:corteplacas} demonstra a transformação de remoção de bordas nas duas modalidades de placas. Essa operação ajuda a destacar a parte da imagem que realmente interessa à análise e remover o restante.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{images/corte-placas.png}
    \caption{Remoção de bordas das placas}
    \label{fig:corteplacas}
\end{figure}

\subsubsection{Binarização}

Com as bordas removidas, é então aplicada binarização na imagem da placa, para obter-se uma versão simplificada na qual os caracteres possuem destaque maior.

Para isso, é aplicada uma técnica de limiarização adaptativa que classifica cada pixel da imagem em escala de cinza como 0 ou 1, a depender do valor do pixel e dos pixels nos arredores. O algoritmo utilizado é o método de Sauvola com os parâmetros $k=0.005$ e o tamanho de janela de 201 px. A utilização desse método evita que placas que não estão iluminadas de forma igual em toda a sua extensão tenham a qualidade da binarização comprometida.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/placa-binarizada.png}
    \caption{Imagem de placa binarizada com o método de Sauvola}
    \label{fig:placabin}
\end{figure}

\subsubsection{Separação dos caracteres}

A separação dos caracteres da imagem da placa binarizada é realizada em duas etapas: Primeiro é feita uma separação estimada, usando as dimensões conhecidas da placa como base, depois a separação é refinada com base nas características da imagem binarizada.

A separação inicial é simples. Sabe-se que cada placa possui 7 caracteres distribuídos de forma uniforme, portanto basta dividir a placa em 7 segmentos de igual largura para se obter uma boa estimativa das divisões dos caracteres, ao menos nas placas do modelo Mercosul. Nas placas do modelo antigo a distribuição não é uniforme por conta do hífen que separa as letras dos números. Para resolver isso, as placas desse modelo primeiro são divididas em duas: uma composta pelas 200 primeiras colunas de pixels da imagem e outra composta pelas 290 últimas colunas. Depois disso, a primeira parte é dividida em três partes iguais e a segunda parte em quatro partes iguais.

Após essa divisão inicial, a divisão é refinada. Como em tese, após a binarização, todo pixel que pertence a um caractere possui valor 1 e os demais pixels possuem valor 0, é feita uma varredura nos arredores do ponto de corte estimado procurando pela coluna com menos pixels com valor 1. No cenário ideal, será encontrado um ponto de corte em que todos os pixels possuem valor 0.

Por fim, a imagem é dividida em 7 pedaços com base nos melhores pontos de corte encontrados. Para padronizar as dimensões, os pedaços são expandidos horizontalmente com pixels de valor 0 até que a largura e altura possuam o mesmo valor.

A figura \ref{fig:sepchars} mostra as etapas e o resultado da separação de caracteres para os dois modelos de placa.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{images/seperacao-chars.png}
    \caption{Separação de caracteres para os dois modelos de placa}
    \label{fig:sepchars}
\end{figure}

\subsubsection{Limpeza das imagens de caracteres}

A última etapa deste bloco é a limpeza das imagens de caracteres extraídas. Muitas dessas imagens acabam sendo geradas com ruído, que não tem nenhuma utilidade e pode atrapalhar a classificação dos caracteres. Esse ruído são pixels que não pertencem ao caractere em questão mas acabam sendo classificados como 1 na binarização.

Quando esses pixels estão desconectados do caractere é possível identificá-los com base na continuidade. Esse processo envolve basicamente classificar cada pixel de valor 1 da imagem como pertencente à um objeto, e em seguida eliminar todos os objetos com exceção daquele com maior área, que deve ser o caractere.

Após essa eliminação as imagens passam a conter consideravelmente menos ruído, como pode ser observado na figura \ref{fig:charclean}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{images/clean-objects.png}
    \caption{Exemplo de limpeza de imagem de caractere com base em continuidade de objetos}
    \label{fig:charclean}
\end{figure}

\subsection{Modelos de classificação de caracteres}

\section{Conversão para execução na placa Rock 3A}

\section{Metodologia de teste}